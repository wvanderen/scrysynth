---
phase: 12-node-catalog-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 12-01
files_modified:
  - src-tauri/src/audio/sequencer.rs
  - src-tauri/src/audio/mod.rs
  - src-tauri/src/audio/supercollider.rs
  - src-tauri/src/audio/runtime_manager.rs
  - src-tauri/src/application/session_store.rs
  - src/components/audio/PrimitivePalette.tsx
  - src/components/session/NodeInspector.tsx
  - src/lib/session-client.ts
  - src/lib/session-client.test.ts
  - src/generated/session-types.ts
  - src-tauri/tests/audio_runtime.rs
autonomous: true
requirements:
  - NODES-04
  - NODES-05

must_haves:
  truths:
    - "A performer can add any catalog node (oscillator/filter/envelope/LFO/utility/effect/step-sequencer) via the palette and it appears in the graph with catalog-declared ports and parameters"
    - "The step sequencer advances through its 16 fixed steps against transport tempo and writes per-step gate+CV to SuperCollider control buses via /c_set"
    - "Every catalog entry's SynthDef /d_recv's successfully to a real booted scsynth (conformance gate, success criterion #4)"
    - "An LFO node routed to a filter's cutoff_cv port produces observable modulation through SuperCollider (D-03 end-to-end)"
    - "The NodeInspector renders catalog-derived parameters, CV ports, and a 16-step gate/CV editor for sequencer nodes"
    - "npm test, npm run build, and cargo test all pass"
  artifacts:
    - path: "src-tauri/src/audio/sequencer.rs"
      provides: "SequencerController — std::thread transport-tick loop sending /c_set per step; spawn/kill lifecycle"
      contains: "SequencerController"
    - path: "src/components/audio/PrimitivePalette.tsx"
      provides: "Catalog-driven node palette iterating NodeCatalogEntry[]; fixes runtimeTarget quirk"
      contains: "NodeCatalogEntry"
    - path: "src/components/session/NodeInspector.tsx"
      provides: "Catalog display name, CV port listing, 16-step sequencer gate+CV editor"
      contains: "sequencerPattern"
    - path: "src-tauri/tests/audio_runtime.rs"
      provides: "scsynth-gated conformance test + CV-bus allocation test + sequencer /c_set advance test + v1 rejection test"
      contains: "/c_set"
  key_links:
    - from: "src/components/audio/PrimitivePalette.tsx"
      to: "src/generated/session-types.ts"
      via: "iterate NodeCatalogEntry[] from generated types; build Node from entry ports/parameters/defaults"
      pattern: "NodeCatalogEntry"
    - from: "src-tauri/src/audio/sequencer.rs"
      to: "src-tauri/src/audio/supercollider.rs"
      via: "OSC /c_set send per sequencer step on allocated gate/CV control buses"
      pattern: "send_message"
    - from: "src-tauri/src/application/session_store.rs"
      to: "src-tauri/src/audio/sequencer.rs"
      via: "spawn SequencerController on audio Ready + transport play; kill on stop/panic"
      pattern: "SequencerController"
---

<objective>
Complete the Phase 12 vertical slice: wire the app-driven step sequencer (NODES-04, D-06/D-07/D-08), make the frontend consume the catalog (NODES-01 success criterion #4 — palette + inspector + Zod), verify end-to-end CV modulation audibly works (NODES-05, D-03), and add the conformance test that boots real `scsynth` for every catalog entry (success criterion #4). After this plan, a performer can add catalog nodes and hear them through SuperCollider, every node exposes modular CV ports, the sequencer advances 16 steps via `/c_set`, and the catalog is the verified single source of truth.

Purpose: Plan 01 delivered the catalog + compiler refactor + SynthDefs + schema. This plan closes the loop — runtime sequencer execution, frontend consumption, and the real-scsynth conformance gate that proves the catalog drives the whole chain. It depends on Plan 01's catalog, SequencerPattern model, SetStepValue command, control-bus allocation, and generated v2 SynthDefs.

Output: New `sequencer.rs` controller, catalog-driven palette/inspector/Zod frontend, regenerated session-types.ts, and the full conformance + invariant test suite passing.
</objective>

<execution_context>
@/Users/eggfam/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/eggfam/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-node-catalog-foundation/12-CONTEXT.md
@.planning/phases/12-node-catalog-foundation/12-RESEARCH.md
@.planning/phases/12-node-catalog-foundation/12-PATTERNS.md
@.planning/phases/12-node-catalog-foundation/12-01-PLAN.md

# Source files being modified (read in full before editing)
@src-tauri/src/audio/supercollider.rs
@src-tauri/src/audio/runtime_manager.rs
@src-tauri/src/application/session_store.rs
@src-tauri/src/domain/session.rs
@src-tauri/src/audio/synthdefs.rs
@src/components/audio/PrimitivePalette.tsx
@src/components/session/NodeInspector.tsx
@src/lib/session-client.ts
@src/lib/session-client.test.ts
@src-tauri/tests/audio_runtime.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: App-driven step sequencer controller + OSC /c_set + lifecycle wiring</name>
  <read_first>
    - src-tauri/src/audio/supercollider.rs (lines 7-8 std::thread/std::time imports, 14-23 module consts, 188-279 apply_resource_plan /s_new arg shape, 286-332 send_live_parameter /n_set OSC send template, 381-403 wait_for_scsynth_boot Instant+thread::sleep periodic pattern, 459-477 resolve_scsynth_executable, 496 ScOscClient, 612 OscTransport trait, 768 apply_resource_plan_sends_groups_then_synths test, 941-981 osc_message helpers, 1031-1086 ScriptedOscTransport)
    - src-tauri/src/audio/runtime_manager.rs (lines 11 AudioRuntimeAdapter trait, 17 set_parameter_value signature, 174-197 reconcile_graph_edit)
    - src-tauri/src/application/session_store.rs (lines 174-202 audio_runtime_manager field + start/stop/panic audio paths, 220-229 poll_hardware_events)
    - src-tauri/src/domain/session.rs (lines 159-175 TransportState, the new SequencerPattern + SetStepValue + Node.sequencer_pattern from Plan 01)
    - src-tauri/src/audio/synthdefs.rs (the CvBusMap / control-bus allocation from Plan 01 — the sequencer's gate_out/cv_out ports get allocated control bus indices here)
    - .planning/phases/12-node-catalog-foundation/12-RESEARCH.md §Step Sequencer (:509-526), §Pattern 3 (:218-232), §Pitfall #6 (:299-303)
  </read_first>
  <files>
    src-tauri/src/audio/sequencer.rs,
    src-tauri/src/audio/mod.rs,
    src-tauri/src/audio/supercollider.rs,
    src-tauri/src/audio/runtime_manager.rs,
    src-tauri/src/application/session_store.rs
  </files>
  <action>
    **Create the app-driven sequencer controller (implements NODES-04; per D-06 app-driven, D-07 mono gate+CV, D-08 fixed 16 steps).**

    Create `src-tauri/src/audio/sequencer.rs` defining `SequencerController`:
    - The controller owns a join handle to a `std::thread::spawn` tick loop (planner decision per research open question 2 + Pitfall #6 recommendation b — std::thread, NOT tokio, which is absent). The loop computes the step period from transport tempo: `Duration::from_secs_f64(60.0 / bpm / 4.0)` (16th notes = step every 60/bpm/4 seconds; 16 steps = one bar of 4/4 — research assumption A2, planner-confirmed clock division). Per step it reads the sequencer Node's `SequencerPattern` at `current_step`, then sends two `/c_set` OSC messages: one for the gate bus (`[gate_bus_index, 1.0 if step.gate else 0.0]`) and one for the CV bus (`[cv_bus_index, step.cv as f32]`). Advance `current_step = (current_step + 1) % 16`. Sleep for the step period. Mirror the `Instant::now()` + `thread::sleep` deadline pattern from supercollider.rs:381-403.
    - The controller needs the OSC client handle and the allocated gate/CV control-bus indices (from Plan 01's CvBusMap) + the transport tempo + the current SequencerPattern. Because the pattern mutates via SetStepValue commands during play, share it via an `Arc<Mutex<SequencerPattern>>` (or an `Arc<RwLock<>>`) that the controller reads each tick and that session_store updates on SetStepValue. Keep it simple — std::sync only.
    - Spawn/kill lifecycle (Pitfall #6): expose `SequencerController::start(osc_handle, gate_bus, cv_bus, tempo_bpm, pattern) -> Self` (spawns the thread) and `stop(self)` (signals shutdown via an `Arc<AtomicBool>` flag the loop checks each iteration, then joins the thread). The loop checks the flag before sleeping so stop is prompt.
    - OSC access: the cleanest route is to add a `tick_sequencer(&self, gate_bus: i32, gate: f32, cv_bus: i32, cv: f32) -> Result<(), String>` method to the `AudioRuntimeAdapter` trait (runtime_manager.rs:11) and implement it on `SuperColliderAdapter` as a `/c_set` send using the existing `osc.send_message` shape (supercollider.rs:318-328 is the `/n_set` template — `/c_set` is the same `vec![OscType::Int(bus), OscType::Float(val)]` arg shape). `/c_set` is fire-and-forget (no /sync needed — RESEARCH.md:249). Alternatively the controller holds a cloned `ScOscClient` directly — choose whichever matches the existing ownership pattern (the trait method is cleaner and testable via FakeAdapter).

    **Wire lifecycle into session_store.rs (application/session_store.rs:174-202):**
    - Add a `sequencer_controllers: HashMap<String, SequencerController>` field (keyed by sequencer node_id; a session may have multiple sequencer nodes) or `Option<SequencerController>` if single is simpler for v1.
    - In the audio-runtime Ready path (after `start_audio_runtime` succeeds and the ScResourcePlan is applied): for each sequencer Node in the session, look up its allocated gate/CV control-bus indices from the applied plan's CvBusMap, read its SequencerPattern, read transport tempo from TransportState, and `SequencerController::start(...)`. Store the handle.
    - In `stop_audio_runtime` / `panic_audio_runtime`: call `.stop()` on every live controller and clear the map. This guarantees the tick thread dies when audio stops or the panic button is hit (control safety — AGENTS.md constraint).
    - Reconcile `GraphEditCommand::SetStepValue` (from Plan 01): in `reconcile_graph_edit` (runtime_manager.rs:174-197), add an arm that updates the shared SequencerPattern (the Arc<Mutex> the controller reads) so live step edits take effect on the next tick. The canonical mutation (SessionDocument) happens before reconcile; reconcile only propagates to the live controller.

    **Module declaration:** add `pub mod sequencer;` to `src-tauri/src/audio/mod.rs`.

    Keep SC dumb (D-06): there is NO sequencer SynthDef. Target nodes read the gate/CV buses via `In.kr` (their SynthDefs from Plan 01 already declare the gate arg / cv_bus args). The sequencer node's catalog entry has `synthdef_name: ""` — the resource planner (Plan 01 Task 2) must skip synthdef/synth allocation for entries with an empty synthdef_name (only allocate the control buses for its gate_out/cv_out ports).
  </action>
  <verify>
    <automated>cd src-tauri && cargo test --lib sequencer 2>&1 | tail -20 && cargo test --test audio_runtime sequencer 2>&1 | tail -20</automated>
  </verify>
  <done>
    - SequencerController exists with start/stop; the tick loop advances current_step 0..15 and wraps; stop() joins the thread within a bounded time.
    - A scripted-OSC unit test (ScriptedOscTransport, supercollider.rs:1031) captures the /c_set packets sent over 16 steps and asserts: 16 gate writes + 16 cv writes, addresses == "/c_set", bus indices match the allocated gate/cv buses, values match the SequencerPattern (gate on/off, cv floats).
    - SetStepValue reconcile updates the live pattern: a step edited mid-play is reflected on the next tick (testable via the shared Arc<Mutex>).
    - start/stop tied to audio lifecycle: spawning on Ready, killed on stop/panic (no orphan threads).
    - cargo test --lib passes (sequencer module + session_store wiring compiles).
  </done>
</task>

<task type="auto">
  <name>Task 2: Frontend catalog consumption — palette + inspector + Zod schemas</name>
  <read_first>
    - src/generated/session-types.ts (the regenerated output from Plan 01 — must contain NodeCatalogEntry, CatalogPortSpec, CatalogParamSpec, NodeCategory, SequencerPattern, and the reshaped Node with nodeTypeId + sequencerPattern)
    - src/components/audio/PrimitivePalette.tsx (lines 1 imports, 34-44 button block, 62-152 buildPrimitiveNode hardcoded factory, 84 runtimeTarget quirk)
    - src/components/session/NodeInspector.tsx (lines 43-49 identity header, 90-98 ports display, 100-127 parameter slider)
    - src/lib/session-client.ts (lines 42-114 Zod nodeSchema + audioPrimitive discriminated union, 413-416 invokeSession parse gate, 434-436 openSessionFromPath)
    - src/lib/session-client.test.ts (lines 13-52 round-trip, 54-82 buildPreviewEffectNode helper)
  </read_first>
  <files>
    src/components/audio/PrimitivePalette.tsx,
    src/components/session/NodeInspector.tsx,
    src/lib/session-client.ts,
    src/lib/session-client.test.ts,
    src/generated/session-types.ts
  </files>
  <action>
    **Make the frontend consume the catalog (implements success criterion #4 — palette/inspector driven from one Rust table; fixes Pitfall #5 Zod drift).**

    **Regenerate session-types.ts:** run the ts-rs contract-write test from Plan 01 (`cargo test write_generated_typescript_contract` in src-tauri/) so `src/generated/session-types.ts` contains the new types. Do NOT hand-edit this file.

    **PrimitivePalette.tsx (src/components/audio/PrimitivePalette.tsx:62-152):** replace the hardcoded `buildPrimitiveNode(kind)` factory and the 3 hardcoded buttons with a catalog iterator:
    - Import `NodeCatalogEntry` (and `Node`, `SessionDocument`) from `../../generated/session-types`.
    - Obtain the catalog list. The catalog is a Rust const; expose it to the frontend via a Tauri command (e.g. `get_node_catalog() -> Vec<NodeCatalogEntry>`) added in lib.rs, OR ship a static export. Prefer a Tauri command so the frontend always reads the compiled-in truth (single source). Add the command to the invoke_handler in lib.rs and call it from the palette (cached on first load). If a command is heavier than warranted for v1, a static generated catalog TS file is acceptable — but the catalog module must remain the source (generate the TS from Rust, do not hand-maintain a second copy).
    - One button per catalog entry; the button label is `entry.displayName`. On click, build a `Node` from the entry: `nodeTypeId: entry.id`, `ports: entry.ports` (map CatalogPortSpec → Port), `parameters: entry.parameters` (map CatalogParamSpec → ParameterValue with default_value), `runtimeTarget: entry.id` (THIS FIXES the v1 `audio/source/${id}` quirk at PrimitivePalette.tsx:84 — runtimeTarget is now the catalog entry id, not a node-id-templated string), `sequencerPattern: entry.id === "step_sequencer" ? { gate: Array(16).fill(false), cv: Array(16).fill(0) } : undefined`.
    - Keep the `disabled={isLoading}` + `onClick={() => addPrimitive(...)}` prop pattern and the existing addPrimitive→GraphEditCommand flow.

    **NodeInspector.tsx (src/components/session/NodeInspector.tsx):**
    - Identity header (:43-49): replace `selectedNode.nodeType` with a catalog display-name lookup (or just render `selectedNode.nodeTypeId`). Use the catalog entry's displayName if available.
    - Parameter slider (:100-127): already generic over `selectedNode.parameters` — works as-is for catalog-derived params. No change to the slider logic.
    - Ports display (:90-98): already iterates `selectedNode.ports` generically — CV ports (Control signalType) added by the catalog render for free. Optionally badge CV ports distinctly (e.g. show "CV" tag on Control-type input ports) for legibility, but the core listing needs no change.
    - Sequencer-step editor (NEW): when `selectedNode.sequencerPattern` is present, render a 16-step grid — 16 gate toggles (checkbox/button, bound to `sequencerPattern.gate[i]`) and 16 CV value sliders (bound to `sequencerPattern.cv[i]`, range e.g. 0..1 or -1..1 per planner discretion). Mirror the existing `.map` slider pattern. Add an `onUpdateStep(nodeId, stepIndex, gate?, cv?)` callback prop that dispatches `GraphEditCommand::SetStepValue` (the TS shape mirrors the Rust variant from Plan 01).

    **session-client.ts Zod schemas (src/lib/session-client.ts:42-114) — fix Pitfall #5:**
    - Relax `nodeSchema`: replace `nodeType: z.enum(["source","effect","mixer","output"])` with `nodeTypeId: z.string()`. Remove the `audioPrimitive: z.discriminatedUnion(...)` block entirely (the model flattens — node identity is the string id, config is parameters). Add `sequencerPattern: z.object({ gate: z.array(z.boolean()).length(16), cv: z.array(z.number()).length(16) }).nullable().optional()`.
    - The port schema (:58-63) is already generic (`signalType: z.enum(["audio","control"])`) — CV ports validate without change.
    - Keep the `invokeSession` parse gate (:413-416) — it now validates the relaxed schema. Every catalog-derived session must round-trip.
    - `openSessionFromPath` (:434-436): unchanged — the friendly v1 message arrives as the rejected string from Plan 01's LegacyV1Session Display.

    **session-client.test.ts:** rebuild `buildPreviewEffectNode` (:54-82) for the new Node shape (nodeTypeId, no nodeType/audioPrimitive). Add a v2 catalog-derived round-trip test: build a session with a catalog node (e.g. oscillator + filter with a CV route + a step_sequencer with a 16-step pattern), serialize through Zod, assert it parses. This is the TS-side invariant test (RESEARCH.md:578).
  </action>
  <verify>
    <automated>npm test -- --run 2>&1 | tail -20 && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>
    - PrimitivePalette renders one button per NodeCatalogEntry (not 3 hardcoded); addPrimitive builds a Node from the entry; runtimeTarget === entry.id (the v1 quirk is gone).
    - NodeInspector renders catalog display name, lists CV ports, and shows a 16-step gate+CV editor for sequencer nodes.
    - session-client.ts Zod schemas use nodeTypeId: z.string() with no closed-enum nodeType and no audioPrimitive union; sequencerPattern schema present.
    - session-client.test.ts has a passing v2 catalog round-trip test.
    - `npm test` passes (vitest).
    - `npm run build` passes (tsc + vite).
    - session-types.ts is regenerated (not hand-edited) and contains NodeCatalogEntry + new types.
  </done>
</task>

<task type="auto">
  <name>Task 3: Conformance test (boots real scsynth per entry) + CV/sequencer/v1-rejection invariant tests</name>
  <read_first>
    - src-tauri/tests/audio_runtime.rs (lines 10-86 deterministic_session, 201 synthdefs mod, 339 resource_plan_fails_loudly, 433 checked_in_v1_synthdef template, 514-585 FakeAdapter lifecycle — the patterns to extend; note Plan 01 already updated deterministic_session + the byte-conformance test)
    - src-tauri/src/audio/supercollider.rs (lines 200-215 /d_recv path, 368 sync_scsynth, 459-477 resolve_scsynth_executable, 26 SuperColliderAdapter::start)
    - src-tauri/src/audio/synthdefs.rs (the CvBusMap + control-bus allocation from Plan 01; the resource plan structure)
    - src-tauri/src/audio/sequencer.rs (the SequencerController from Task 1 — for the /c_set advance test)
    - src-tauri/src/persistence/session_file.rs (the two-phase parse + LegacyV1Session from Plan 01)
    - .planning/phases/12-node-catalog-foundation/12-RESEARCH.md §Testable Invariants (:566-579), §Code Examples conformance template (:363-374)
  </read_first>
  <files>
    src-tauri/tests/audio_runtime.rs
  </files>
  <action>
    **Add the real-scsynth conformance gate + all phase invariant tests (implements success criterion #4; encodes the RESEARCH.md Testable Invariants table :566-579).**

    **Conformance test — boots real scsynth for every catalog entry (success criterion #4, the phase lynchpin):**
    - Add `#[test] #[ignore = "requires local scsynth bundle"] fn every_catalog_entry_synthdef_loads_on_real_scsynth()`. Gate the body on `resolve_scsynth_executable()` (supercollider.rs:459) returning `Some` — if `None`, print a clear skip message and return (the scsynth bundle is at `/Applications/SuperCollider.app/Contents/Resources/scsynth`, not on PATH; `SCRYSYNTH_SCSYNTH_PATH` env or the bundle fallback resolves it — RESEARCH.md:614). Do NOT fail when scsynth is absent; the test is opt-in via `cargo test -- --ignored`.
    - When scsynth is available: boot it via `SuperColliderAdapter::start` (supercollider.rs:26), then for EACH `NodeCatalogEntry` in `CATALOG` with a non-empty `synthdef_resource`: read the `.scsyndef` bytes, `/d_recv` them (supercollider.rs:200-215 pattern), assert the `/done` acknowledgement via `sync_scsynth` (supercollider.rs:368), then `/d_free` to clean up. Assert zero failures across all entries. On any `/d_recv` failure, the test reports which entry + which UGen was rejected (catches unavailable UGens like FreeVerb/GVerb/CombC per research assumption A3). Tear down scsynth at the end.
    - This is THE test that proves the catalog drives the full chain end-to-end on the real audio engine.

    **CV-bus allocation + end-to-end modulation test (NODES-05, D-03):**
    - Extend the resource-plan mapping test: build a session with an LFO node routed (Control-type route) to a Filter node's `cutoff_cv` port. Assert the resulting `ScResourcePlan` allocates a control bus (index >= FIRST_CONTROL_BUS_OFFSET=1024), assigns `out_cv_bus` arg to the LFO synth and `cutoff_cv_bus` arg to the Filter synth. This verifies the compiler artifact wiring (RESEARCH.md:575).
    - For the audible/observable end-to-end assertion (D-03 "audibly modulates"): if scsynth is available (same gate as the conformance test, `#[ignore]`), boot it, apply the LFO→filter plan, run briefly, and assert the filter's cutoff control-bus value changes over time (read via `/c_get` or assert the synth launched with the cv bus arg). A full audible FFT assertion is out of scope; the wiring + launch verification is the gate. If scsynth absent, the unit-level bus-allocation assertion stands alone.

    **Sequencer /c_set advance test (NODES-04, D-06/D-07/D-08):**
    - Using `ScriptedOscTransport` (supercollider.rs:1031), capture the OSC packets a `SequencerController` sends over a full 16-step cycle. Assert: exactly 32 `/c_set` messages (16 gate + 16 cv), the gate values match `SequencerPattern.gate[i]`, the cv values match `SequencerPattern.cv[i]`, bus indices match the allocated gate/cv buses, and step index wraps 15→0. Mirror the `apply_resource_plan_sends_groups_then_synths_in_plan_order` (supercollider.rs:768) capture-and-assert shape, using the `osc_message_addr`/`osc_message_int_arg`/`osc_message_float_arg` helpers (supercollider.rs:941-981).

    **Port-type mismatch rejection test (Pitfall #3):**
    - Build a route from an Audio-type port to a Control-type CV port (rate mismatch) and assert `validate_routes` (compiler.rs:194) returns the port-type-mismatch `TopologyCompileError` variant added in Plan 01. This guards against the silent SC rate-mismatch failure.

    **v1 rejection test (D-09/D-10):**
    - Write a temp JSON file with `{"schemaVersion":1, ...}` (minimal valid v1-shaped content) and assert `open_session_from_path` returns `Err(SessionFileError::LegacyV1Session)` whose Display string is the friendly "This is a v1 session — unsupported in Scrysynth v2..." message — NOT a serde Deserialize error (Pitfall #1 guard). Also assert a `{"schemaVersion":2,...}` file with a missing required field still produces a clear deserialize error (two-phase parse only short-circuits on version mismatch).

    **Catalog-replaces-allowlists invariant (success criterion #3, the "adding a node = catalog edit only" proof):**
    - Add a test that asserts an unknown `node_type_id` (e.g. "nonexistent_node") returns `Err(ScResourcePlanError::UnknownCatalogEntry)` through the full `compile_session_to_topology` → `plan_sc_resources` path — proving no panic and that the catalog is the gate. (Plan 01's resource_plan_fails_loudly update covers the unit; this extends it through the compiler entry point.)
  </action>
  <verify>
    <automated>cd src-tauri && cargo test --test audio_runtime 2>&1 | tail -30 && cargo test --test audio_runtime -- --ignored 2>&1 | tail -30 ; echo "---NPM---" ; npm test -- --run 2>&1 | tail -10 && npm run build 2>&1 | tail -10</automated>
  </verify>
  <done>
    - `cargo test --test audio_runtime` passes (all non-ignored tests: CV-bus allocation, sequencer /c_set advance, port-mismatch rejection, v1 rejection, unknown-id Err, byte-conformance from Plan 01).
    - `cargo test --test audio_runtime -- --ignored` passes WHEN scsynth is available (the every_catalog_entry_synthdef_loads_on_real_scsynth conformance gate; skips cleanly with a message when scsynth is absent). Every catalog entry's synthdef /d_recv's successfully.
    - `npm test` passes (vitest, including the v2 catalog round-trip).
    - `npm run build` passes (tsc + vite — frontend compiles against regenerated session-types.ts).
    - All four Phase 12 success criteria from ROADMAP.md are verifiably met by this plan + Plan 01.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Frontend → Tauri commands | Catalog read + GraphEditCommand dispatch cross here (typed commands, existing ownership gates) |
| App → scsynth process | /c_set writes + /d_recv cross here (local loopback, compiled-in synthdefs) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-06 | Denial | sequencer.rs SequencerController tick thread | mitigate | AtomicBool shutdown flag + thread::join on stop/panic; no orphan threads (control safety) |
| T-12-07 | Tampering | session_store SetStepValue reconcile | mitigate | Mutations flow through typed GraphEditCommand gate (existing ownership/approval gates unchanged); shared pattern behind Arc<Mutex> |
| T-12-08 | Repudiation | sequencer /c_set writes | accept | Fire-and-forget local OSC to dumb scsynth; no audit trail needed for local single-user instrument |
| T-12-09 | Tampering | PrimitivePalette catalog-driven Node construction | mitigate | Node built from compiled-in catalog entry defaults; runtimeTarget = entry.id (fixes the v1 node-id-templating quirk that could mismatched routes) |
| T-12-SC | Tampering | (no package installs this phase) | accept | No new crates/npm packages — Package Legitimacy Gate trivially satisfied |
</threat_model>

<verification>
- `cargo test` (lib + audio_runtime, non-ignored) passes: catalog invariants, CV-bus allocation, sequencer /c_set advance, port-mismatch rejection, v1 rejection, unknown-id Err.
- `cargo test -- --ignored` passes when scsynth bundle is present: every catalog entry's synthdef loads on real scsynth (the Phase 12 lynchpin, success criterion #4).
- `npm test` + `npm run build` pass: frontend consumes regenerated session-types.ts; Zod schemas relaxed; catalog round-trips.
- Manual smoke (optional, human-verify if desired): launch the app, add an oscillator + filter + LFO via the palette, route LFO→filter cutoff_cv, start transport, confirm audible modulation; add a step_sequencer, confirm 16-step advance.
- All four ROADMAP success criteria verifiable: (1) performer adds nodes + hears them [palette+catalog+synthdefs+SC], (2) every node exposes CV ports [catalog D-04/D-05], (3) adding a node = catalog edit only [5-spot refactor + Err], (4) conformance test boots real scsynth per entry [Task 3].
</verification>

<success_criteria>
- NODES-04: step sequencer node exists; SequencerController advances 16 steps against transport; writes /c_set per step; mono gate+CV (D-07). Verified by the /c_set advance test.
- NODES-05 (end-to-end half): LFO→filter CV route allocates control buses + launches with cv_bus args; modulation is observable through SC. Completes the NODES-05 work started in Plan 01 (port declaration).
- Success criterion #1: performer can add oscillator/filter/envelope/LFO/utility/effect/step-sequencer nodes and hear them. Verified by catalog-driven palette + conformance test + regenerated synthdefs.
- Success criterion #4 (full): conformance test boots real scsynth for every entry.
- npm test, npm run build, cargo test all green.
</success_criteria>

<output>
Create `.planning/phases/12-node-catalog-foundation/12-02-SUMMARY.md` when done.

## Artifacts this phase produces (for source-grounding / drift verification)

**New Rust module/type:**
- `crate::audio::sequencer` module (src-tauri/src/audio/sequencer.rs)
- `SequencerController` struct (std::thread tick loop + Arc<Mutex<SequencerPattern>> + AtomicBool shutdown + join handle)
- `AudioRuntimeAdapter::tick_sequencer(...)` trait method (runtime_manager.rs) — or equivalent OSC-handle accessor

**New Tauri command (if chosen over static export):**
- `get_node_catalog() -> Vec<NodeCatalogEntry>` (lib.rs invoke_handler) — exposes the compiled-in catalog to the frontend

**New frontend surface:**
- Catalog-driven PrimitivePalette (iterates NodeCatalogEntry[])
- NodeInspector sequencer-step editor (16 gate toggles + 16 CV sliders) + onUpdateStep callback
- Relaxed session-client.ts Zod schemas (nodeTypeId: z.string(), no audioPrimitive union, sequencerPattern schema)

**New/extended tests (src-tauri/tests/audio_runtime.rs):**
- `every_catalog_entry_synthdef_loads_on_real_scsynth` (#[ignore], scsynth-gated conformance — success criterion #4)
- CV-bus allocation + LFO→filter modulation test
- Sequencer /c_set 16-step advance test (ScriptedOscTransport)
- Port-type mismatch rejection test
- v1 rejection (LegacyV1Session) test
- Catalog-replaces-allowlists (unknown-id Err through compiler) test

**Regenerated:** src/generated/session-types.ts (from Plan 01's ts-rs export; consumed here)

(No new SynthDefs, no new error variants, no schema change in this plan — those are Plan 01 artifacts this plan consumes.)
</output>
